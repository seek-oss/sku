import { style } from '@vanilla-extract/css';

/** Fixture stylesheet for SSR start virtual:ssr-css collection. */
export const root = style({
  fontFamily: 'sku-ssr-document-layout, system-ui, sans-serif',
});
