{
  "name": "@sku-fixtures/security-controls",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "sku build",
    "build:vite": "sku build --config sku.config.vite.ts",
    "build:ssr": "sku build-ssr --config sku-server.config.ts",
    "start": "sku start",
    "serve:assets": "http-server dist-ssr --cors",
    "start-ssr:vite": "sku start-ssr --config sku-server.config.ts",
    "start:vite": "sku start --config sku.config.vite.ts"
  },
  "dependencies": {
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@sku-private/test-utils": "workspace:*",
    "@vanilla-extract/css": "^1.0.0",
    "dedent": "^1.5.1",
    "sku": "workspace:*",
    "http-server": "^14.1.1"
  },
  "skuSkipValidatePeerDeps": true
}
