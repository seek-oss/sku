---
'sku': patch
---

Re-enable `vite-plugin-cjs-interop` dependency

Previously, this package was internalised due to a bug. Now that the issue has been resolved upstream, we're reverting to using the official package.
