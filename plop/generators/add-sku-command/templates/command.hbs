const { Command } = require('commander');

const {{camelCase commandName}} = new Command('{{commandName}}');

{{camelCase commandName}}
  .description('<YOUR_DESCRIPTION_HERE>')
  .argument('<ARGUMENTS_HERE>', '<ARGUMENT_DESCRIPTION>')
  .action(() => {
    const {{camelCase commandName}}Action = require('./{{commandName}}.action');
    {{camelCase commandName}}Action();
  });

module.exports = {{camelCase commandName}};
