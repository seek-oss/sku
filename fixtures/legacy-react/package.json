{
  "name": "@sku-fixtures/legacy-react",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "sku start",
    "COMMENT": "echo "
  },
  "comments": {
    "react": "A fake react dependency is injected during the test so we don't need to install the real react"
  },
  "devDependencies": {
    "sku": "workspace:*"
  },
  "skuSkipValidatePeerDeps": true
}
