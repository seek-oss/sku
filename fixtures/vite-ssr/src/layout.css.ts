import { style } from '@vanilla-extract/css';

/** Fixture stylesheet for Vite SSR start virtual:ssr-css collection. */
export const root = style({
  fontFamily: 'sku-vite-ssr-layout, system-ui, sans-serif',
});
