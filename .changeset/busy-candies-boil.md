---
'@sku-lib/create': minor
---

When `pnpm` is used to run the CLI, the `package.json#packageManager` field will now be set to the latest v11 version of PNPM and a `pnpm-workspace.yaml` will be included in directory root
