---
'pnpm-plugin-sku': minor
---

Expose a `pnpm-plugin-sku/config` entrypoint that exports the default PNPM config used by the plugin

**EXAMPLE USAGE**:

```ts
import { defaultConfig } from 'pnpm-plugin-sku/config';
```
